let meuArray = [];

// push & unshift
meuArray.push("Thiago");
meuArray.unshift("Thiago 2");

// pop & shift
meuArray.pop();
meuArray.shift();

// splice (edit)
meuArray.splice(1, 1);

// slice (copy)
meuArray.slice(1, 1);

// indexOf
meuArray.indexOf('Thiago');

// join
meuArray.join(',')

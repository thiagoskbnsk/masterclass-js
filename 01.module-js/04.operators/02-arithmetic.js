// soma
const soma = 1 + 1;

// multiplicacao
const multiplacao = 2 * 2;

// divisao
const divisao = 10 / 5;

// subtracao
const subtracao = 1 - 1;

// resto
const resto = 10 % 2; // 0
// 10 / 2 = 5; e o que sobra é 0;

//exemplo de verificacao se o numero eh par
const numero = 13;
const ehPar = numero % 2 === 0

// incremento
let numero = 0;
// numero = numero + 1; // 1
// numero = numero + 1; // 2

// numero += 5;

// numero++;
// ++numero;

// decremento
let numero = 0;
numero = numero - 1;
numero = numero - 1;

// numero -= 5;

// numero--;
// --numero;

// exponencial
let novoNumero = 2;

novoNumero ** 4; // 2 * 2 * 2 * 2 = 16